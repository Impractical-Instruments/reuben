#!/bin/sh
# Every check registered under .githooks/pre-push.d/, in name order. The logic is in `dispatch`;
# this file exists only because git looks a hook up by its exact name.
exec "$(dirname -- "$0")/dispatch" pre-push "$@"
