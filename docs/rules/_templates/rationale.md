# Why: <the rule, named or restated>

[Rule](../../<topic>.md#<rule-slug>)

<The condensed reasoning that still applies — the forces that make the rule the right call.
Drop superseded alternatives and dead-end history; git keeps them. As long as it needs to be
and no longer. This is the deepest rung: it loads only when someone needs the why.>

Distilled from: ADR-00xx[, ADR-00yy]
