{
  "doc": "One-shot trigger sampler (ADR-0016). A note's gate fires the 'blip' sample; its pitch shifts the playback rate around root MIDI 57 (the sample's recorded pitch, ~220 Hz). Send /voicer/notes [midi, gate] to play (e.g. [57.0, 1.0] plays at natural pitch; [69.0, 1.0] an octave up). 4 voices.",
  "instrument": "sampler",
  "nodes": [
    {
      "address": "/voicer",
      "config": {
        "voices": 4
      },
      "doc": "Note -> per-Voice freq + gate CV.",
      "type": "voicer"
    },
    {
      "address": "/sample",
      "doc": "Gate edge fires the sample; freq sets the playback rate.",
      "inputs": {
        "freq": {
          "from": "/voicer.freq"
        },
        "gate": {
          "from": "/voicer.gate"
        },
        "root": 57.0
      },
      "sample": "blip",
      "type": "sample"
    },
    {
      "address": "/out",
      "inputs": {
        "audio": {
          "from": "/sample"
        }
      },
      "type": "output"
    }
  ],
  "outputs": [
    {
      "node": "/out",
      "port": "audio"
    }
  ],
  "resources": {
    "blip": "samples/blip.wav"
  }
}
